## Read the help page for read.table, many hints, super vaulable ##
## dput saves r objects to a file
## dget reads the file back in to r
myfunction <- function () (
  x<- rnorm(100)
  mean(x)
  )

  